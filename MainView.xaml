<Window x:Class="RoomNumberingPlugin.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoomNumberingPlugin"
             mc:Ignorable="d" 
             Height="150" Width="350"
             Title="Нумерация помещений">

    <StackPanel>
        <Label Content="Введите начальный номер для помещений на этаже"
               Margin="5"/>
        <TextBox Text="{Binding FirstNumber}"
                 Margin="5"
                 Padding="5"/>

        <Button Content="Применить"
                Command="{Binding NumberingCommand}"
                Margin="5"
                Padding="5"/>
    </StackPanel>
</Window>
